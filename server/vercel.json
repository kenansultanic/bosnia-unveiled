{
    "builds": [
      {
        "src": "server/server/wsgi.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "server/server/wsgi.py"
      }
    ]
}