import "./gallery.scss";
import { createPortal } from "react-dom";

const Gallery = () => {
    return (
        <div className="gallery">
            
        </div>
    );
};

export default Gallery;